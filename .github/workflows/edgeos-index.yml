name: Index EdgeOS Reference Content

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  index:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: skills/edgeos
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - run: bun install --frozen-lockfile

      - run: bun run scripts/index.ts

      - name: Commit updated references
        working-directory: ${{ github.workspace }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add skills/edgeos/references/
          git diff --cached --quiet || git commit -m "Update EdgeOS reference content [automated]"
          git push
